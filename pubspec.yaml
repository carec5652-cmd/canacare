name: admin_can_care
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1
environment:
  sdk: ^3.7.2
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_messaging: ^15.1.3
  cloud_functions: ^5.1.3
  flutter_local_notifications: ^17.2.3
  http: ^1.2.0
  pinput: ^5.0.2
  get: ^4.7.2
  # UI dependencies
  awesome_dialog: ^3.2.0
  flutter_animate: ^4.5.0
  provider: ^6.1.1
  # Image and PDF dependencies (keep if needed)
  image_picker: ^1.0.7
  pdf: ^3.10.7
  printing: ^5.11.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
  assets:
    - images/logo-removebg-preview.png
